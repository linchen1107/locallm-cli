# Set default behavior, in case users don't have core.autocrlf set.
# This will handle line endings automatically based on file content.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.py text
*.txt text
*.md text
*.json text
*.html text
*.css text
*.js text
*.sql text
*.yml text
*.yaml text
*.toml text
*.gitignore text
*.gitattributes text

# Declare files that will always have LF line endings, common for shell scripts.
*.sh text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.encrypted binary
*.backup.* binary
